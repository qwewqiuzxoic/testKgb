import React from 'react';

function Title({title, subTitle}) {
  return (
    <div>
      <div>
          {title}
      </div>
      <div>
          {subTitle}
      </div>
    </div>
  );
}

export default Title;